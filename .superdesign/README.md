# Superdesign handoff

Use `docs/SUPERDESIGN_CANVAS_BRIEF.md` as the setup answer and design constraints.

The current code implementation is the source of truth. A static canvas seed is available at:

```text
.superdesign/replica_html_template/portfolio-home.html
```

The replica is only a visual seed for the canvas. Do not copy its static text back into the application. The real bilingual content remains in `src/content` and `src/i18n`.
