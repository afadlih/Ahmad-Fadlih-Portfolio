# Canonical production origin. Use HTTPS and omit the trailing slash.
# Required only for release and deployment checks.
NEXT_PUBLIC_SITE_URL=

# Server-only flag. Keep false in production.
ENABLE_CONTENT_STUDIO=false

# Public origin used by the scheduled production route check.
PORTFOLIO_PUBLIC_URL=
