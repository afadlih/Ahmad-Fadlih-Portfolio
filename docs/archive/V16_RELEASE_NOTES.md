# V16 release notes

## What changed

- audited and synchronized the global palette for light mode and dark mode;
- refined header, card, button, tag, and section surfaces so the site feels consistent;
- localized the theme toggle and kept the state in local storage;
- replaced the portfolio portrait with the latest professional studio-style headshot;
- improved readability across the resume page, selected work deck, and system map surfaces.

## Result

The portfolio now opens in a cleaner light theme, supports a deliberate dark theme, and uses a tighter cobalt + teal palette with a warmer accent for emphasis.
